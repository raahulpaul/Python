l1 = [1, 8, 7, 2, 21, 15]
# l1_sorted = l1.sort()
# print(l1_sorted) # It'll give none bcoz in python it changes l1 not l1_sorted

# Instead of the upper case, use the below one..
# l1.sort() # Sorts the list
# l1.reverse() # Reverses the list
# l1.append(45) # adds 45 at the end of the list
# l1.insert(0, 544) # adds 544 at the location of index 0.
# l1.pop(2) # will delete element at index 2
# l1.remove(21) # will remove 21 from the list
print(l1)